# Optional. Defaults to PublicNode, then hops if an endpoint blocks the browser.
# Official api.mainnet-beta.solana.com often 403s GitHub Pages / localhost Origins.
VITE_RPC_URL=https://solana-rpc.publicnode.com
